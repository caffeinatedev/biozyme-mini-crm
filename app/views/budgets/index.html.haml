.container.mt-4
  .d-flex.justify-content-between.mb-3
    %h2 Budgets
    = link_to "New Budget", new_budget_path, class: "btn btn-primary"

  %table.table.table-striped
    %thead
      %tr
        %th Contact
        %th Year
        %th Amount
        %th Actions
    %tbody
      - @budgets.each do |budget|
        %tr
          %td= budget.contact.name
          %td= budget.year
          %td= number_to_currency(budget.amount_cents / 100.0)
          %td
            = link_to "Edit", edit_budget_path(budget), class: "btn btn-sm btn-outline-primary"
            = link_to "Delete",
              budget_path(budget),
              method: :delete,
              data: { confirm: "Are you sure?" },
              class: "btn btn-sm btn-outline-danger"